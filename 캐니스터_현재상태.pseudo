// 언제든지 특정 캐니스터의 상태를 확인할 수 있는 간단한 조회함수 입니다.



// 특정 캐니스터의 현재 상태를 반환하는 함수
FUNCTION get_canister_status(canister_id):

    // 1. DB 조회: 데이터베이스에서 해당 ID의 캐니스터 정보를 가져옴
    canister_data = database.lookup_canister(canister_id)

    // 2. 정보 반환: 조회된 상태와 위치 정보를 반환
    IF canister_data is NOT NULL THEN
        RETURN {
            status: canister_data.status,
            location: canister_data.location
        }
    ELSE
        RETURN "오류: 해당 ID의 캐니스터를 찾을 수 없음"
    END IF